#[[
    TODO(will) select which subdir to add based on build command line parameter
               (for ease of setup when supporting NDA platforms, it must match the folder name)
#]]

add_subdirectory(win64)
